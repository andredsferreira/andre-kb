# Very simple example of a Role object. Keep in mind that Kubernetes
# already comes with a bunch of default roles.

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: role-01
  namespace: default
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get"]
