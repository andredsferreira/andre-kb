# TargetGroupConfiguration manifest (assumes AWS ALB controller is installed on
# the cluster).

apiVersion: gateway.k8s.aws/v1beta1
kind: TargetGroupConfiguration
metadata:
  name: my-app-service-tgc
  namespace: test
spec:
  targetReference:
    name: my-app-service
  defaultConfiguration:
    targetType: ip
