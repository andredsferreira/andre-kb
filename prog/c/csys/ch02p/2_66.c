// 0xFF -> 0x8000
// 1111 1111 -> 1000 0000 0000 0000

// 0x6600 -> 0x4000
// 0110 0110 0000 0000 -> 0100 0000 0000 0000

int leftmost_one(unsigned x) {

  x &= ~(x >> 1);
  x &= ~(x >> 2);
  x &= ~(x >> 4);
  x &= ~(x >> 8);
  x &= ~(x >> 16);

  return x;
}
